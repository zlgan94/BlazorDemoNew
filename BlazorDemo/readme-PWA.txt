01. Add the ASP.NET CORE WEB APP project
02. Add the manifest.json
03. Generate the favicons for the different resolutions
04. Added the images to the project,
and define them in the manifest.json,
and copy "favicon.ico" file to the "wwwroot" folder
05. Add the Service Worker "sq.js" to the "wwwroot" folder
06. Add the Register Service Worker Install Script "app-sw.js"
07 Add the HTML elements for "Add-to-Home" (A2H) to the first pageo f the application 
"Index.cshtml"
Add the references to "app-sw.js"
Add the handler for the button to install the PWA application ("/js/addtohome.js")